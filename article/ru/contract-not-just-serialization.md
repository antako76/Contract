# CONTRACT: одна схема вместо N×M сериализаторов

**One contract. Any format. Zero mapping.**

Любой C++-проект, где данные не остаются внутри одного процесса, рано или
поздно упирается в одно и то же: одну и ту же структуру нужно уметь показать
в дебаге, записать в бинарный протокол, отдать по protobuf во внешний сервис
и залогировать в JSON. Без общего механизма это превращается в
`toJson`/`toProto`/`debugPrint`/`writeBinary` - россыпь ручных мэппингов на
каждый тип, которые с каждым изменением схемы нужно синхронизировать руками:

```text
N классов данных × M форматов = N×M ручных мэппингов
```

Мы не единственные, кто в C++ уперся именно в эту стену - рядом стоят
[reflect-cpp](https://github.com/getml/reflect-cpp) (C++20-рефлексия,
JSON/BSON/CBOR/msgpack/TOML/XML/YAML/Avro/Cap'n Proto и другие),
[serde-cpp](https://github.com/GerHobbelt/serde-cpp) (вдохновлен Rust serde)
и более старые Boost.Serialization/Cereal. Так что боль настоящая, не
придуманная под статью - вопрос в том, что именно предлагает CONTRACT в
дополнение к "одна схема → много форматов", раз эта идея уже не нова.

## Не еще один сериализатор

CONTRACT решает N×M иначе, чем библиотека сериализации:

```text
N контрактов + M адаптеров
```

Схема объявляется один раз, прямо в C++-структуре:

```cpp
struct Order {
    std::uint64_t id;
    std::string customer;
    double amount;
    bool paid;

    CONTRACT(Order,
        (id, 1),
        (customer, 2),
        (amount, 3),
        (paid, 4)
    )
};
```

`CONTRACT(...)` не сериализует ничего сам. Он дает стабильный список полей -
id, имя, тип, порядок обхода - которым может воспользоваться сериализатор, а
может и что-то другое: валидатор, экспортер схемы, аудит-дамп. При
проектировании мы сразу определили эту границу: CONTRACT - не
рантайм-рефлексия, не generic-сериализатор и не schema-first кодогенератор.
Ядро отвечает за то, что такое поле и как до него добраться, а не за то, как
оно должно выглядеть в wire-формате - это уже задача адаптера.

Отсюда и разница с reflect-cpp: reflect-cpp отвечает на вопрос "как
сериализовать структуру в N форматов". CONTRACT отвечает на другой вопрос -
"как дать структуре стабильный контракт, которым сериализация может
воспользоваться, а может и не воспользоваться".

## Было / стало

Без общей схемы `Order` из примера выше выглядела бы примерно так:

```cpp
struct Order {
    std::uint64_t id;
    std::string customer;
    double amount;
    bool paid;

    std::string toJson() const {
        std::ostringstream out;
        out << "{\"id\":" << id
            << ",\"customer\":\"" << customer << "\""
            << ",\"amount\":" << amount
            << ",\"paid\":" << (paid ? "true" : "false") << "}";
        return out.str();
    }

    void debugPrint(std::ostream& out) const {
        out << "Order{id=" << id << ", customer=" << customer
            << ", amount=" << amount << ", paid=" << paid << "}";
    }

    void writeBinary(std::vector<std::uint8_t>& buf) const { /* ... */ }
};
```

Плюс отдельный `order.proto`, `protoc`, сгенерированные `.pb.h`/`.pb.cc` и
ручной `toProto`/`fromProto` между `Order` и `OrderProto`. Четыре формата -
четыре места, куда нужно не забыть внести любое изменение схемы.

С CONTRACT `Order` объявляется один раз (см. выше), а дальше формат - это
просто выбор адаптера:

```cpp
contract::cout << order;
contract::adapters::json::to_string(order);
binary_out << order;
proto_out << order;
```

`Order` в этом коде не меняется вообще - меняется только то, через что его
пропускают.

Разница видна и в обратную сторону: если нужно добавить новое поле, в
`CONTRACT(...)` это одна строка. В "было"-варианте это правка сразу в
нескольких местах - `toJson`, `debugPrint`, `writeBinary`, `.proto`-файле и
ручном `toProto`/`fromProto` - и в каждом легко забыть.

## Чего мы хотели от модели

Стоп - на этом простом примере уже вырисовывается модель шире, чем "одна
декларация вместо N мэппингов". Хотелось, чтобы:

1. Контракт был стабильной схемой, объявленной один раз в самом C++-типе -
   независимо от формата:
   - id
   - имя
   - тип
   - способ доступа к полю.
2. Поле не обязано было быть физическим членом структуры:
   - переиспользовать схему через наследование (`BASE`)
   - вычислять значение на лету (`PROPERTY`)
   - ссылаться на данные, которыми тип не владеет (`REFERENCE`).
3. Формат и поведение целиком принадлежали адаптеру, а не ядру -
   сериализация как одна из возможных ролей, не единственная:
   - сериализация (protobuf, JSON, compact, binary, ...)
   - валидация
   - экспорт схемы
   - аудит-дамп.
4. Поверх полей был отдельный слой атрибутов - политика, которую разные
   адаптеры трактуют по-своему:
   - security
   - check
   - unit.
5. Все это не создавало излишнюю нагрузку на рантайм.

Дальше - как каждый пункт получился на практике.

## Ядро контракта

Первый пункт - стабильная схема с id, именем, типом и способом доступа к
полю. На практике это небольшой compile-time API, поверх которого построено
все остальное:

```cpp
contract::field_count<Order>();                        // сколько полей
contract::field_at<0, Order>();                        // дескриптор поля по индексу
contract::dispatch_field_by_id<Order>(2, fn);          // найти поле по id
contract::dispatch_field_by_name<Order>("amount", fn); // то же самое по имени
contract::type_name<Order>();                          // "Order"
```

Дескриптор поля несет id, имя и способ доступа (`get`/`set`/`ref`) - этого
достаточно, чтобы адаптер построил вокруг него что угодно, от wire-кодека до
дебаг-дампа, ни разу не заглянув внутрь самой структуры напрямую.

## Гибкость контракта: BASE, PROPERTY и REFERENCE

Второй пункт списка - поле не обязано быть физическим членом структуры. У
CONTRACT для этого есть три механизма.

**`BASE(Type, offset)`** подключает контракт другого C++-типа как часть
текущего через обычное наследование, со сдвигом id, чтобы поля базового
типа не столкнулись с полями производного:

```cpp
struct Header {
    std::uint64_t request_id;

    CONTRACT(Header, (request_id, 1))
};

struct Event : public Header {
    std::string name;

    CONTRACT(Event,
        BASE(Header, 100),
        (name, 1)
    )
};
```

`Event` получает `request_id` под id 101 (100 + 1) и свое `name` под id 1 -
общая часть схемы объявлена один раз в `Header` и переиспользуется, а не
копируется в каждый тип, где она нужна.

**`PROPERTY(name, id, type)`** - поле контракта, за которым не стоит
физический член структуры, а стоит пара `contract_get`/`contract_set`:

```cpp
struct Metric {
    std::uint32_t raw_count = 0;

    CONTRACT(Metric,
        (raw_count, 1),
        PROPERTY(doubled_count, 2, std::uint32_t)
    )

    std::uint32_t contract_get(const contract_fields::doubled_count&) const {
        return raw_count * 2;
    }

    void contract_set(const contract_fields::doubled_count&, std::uint32_t value) {
        raw_count = value / 2;
    }
};
```

Адаптеры видят `doubled_count` как обычное поле - читают и пишут его тем же
путем, что и `raw_count`, хотя в памяти `Metric` такого поля вообще нет:
значение вычисляется на лету через `contract_get`/`contract_set`.

**`REFERENCE(name, id)`** - третий вид поля: контракт на данные, которыми
структура не владеет, а только ссылается. Это не синтетический пример -
структурный логгер CONTRACT так и устроен, чтобы не копировать значение на
горячем пути:

```cpp
template<class T>
struct payload_field {
    std::string_view name;
    const T& value;

    CONTRACT(payload_field,
        (name, 1),
        REFERENCE(value, 2)
    )
};
```

`value` - ссылка, а не копия; адаптер читает ее как обычное поле контракта,
но лог-вызов не платит за аллокацию/копирование логируемого значения.

## Экосистема адаптеров

Третий пункт - формат и поведение принадлежат адаптеру, а не ядру. Сегодня
в CONTRACT шесть семейств адаптеров, и не все из них симметричны по
чтению/записи. Ниже - по убыванию значимости и полноты реализации:

| Адаптер | Запись | Чтение | Комментарий |
|---|---|---|---|
| protobuf | ✓ | ✓ | полный: wire-совместим с настоящим protobuf, обгоняет libprotobuf в 20/28 замеров ([отдельная статья](protobuf-adapter-performance.md)) |
| binary | ✓ | ✓ | полный: нативная раскладка без wire-оверхеда, самый быстрый вариант - но не кросс-платформенный формат по умолчанию |
| compact | ✓ | ✓ | полный: свой компактный wire-формат, единственный, кто сегодня реально пропускает незнакомые поля при чтении |
| JSON | ✓ | - | только запись, зато с security-режимами (redact/omit) - на нем построен structured logging |
| structured logging | ✓ | - | тонкая надстройка над JSON-адаптером для логов, не отдельный wire-формат |
| console/debug | ✓ | - | человекочитаемый дебаг-вывод |
| YAML | - | ✓ | только чтение: строгий config-reader, а не экспортный формат - писать в YAML CONTRACT пока не умеет |

Общая для всех модель одна и та же: `contract` знает поля и их идентичность,
`io` - байты и курсор, `writer`/`reader` - обход, `codec<T>` - форму
конкретного типа внутри конкретного адаптера. Ни один из этих слоев не
завязан на конкретный формат - именно это позволяет добавлять новый адаптер,
не трогая уже существующие.

## Эволюция схемы

Еще один пример того же третьего пункта - что делать при изменении схемы,
тоже решает адаптер, а не ядро. Номера полей - не значит, что по именам так делать было бы вообще
невозможно, просто дороже: длиннее на wire, дольше сравнивать. Но реальная
проблема эволюции схемы острее размера: переиспользовать один и тот же
идентификатор для поля с несовместимым типом - и старый, и новый код
начнут по-разному трактовать одни и те же байты. Для этого в контракте
можно явно зарезервировать id:

```cpp
CONTRACT(Order,
    ATTRS(
        contract::schema::reserved_id(2)   // customer больше не существует под id=2
    ),
    (id, 1),
    (customer_id, 3)
)
```

Это пока декларативный маркер - сам он ничего не проверяет, enforcement на
стороне адаптера. А что делать с полем, которого контракт вообще не знает
- пропустить или упасть - решает сам адаптер: compact сегодня пропускает,
protobuf падает с ошибкой.

## Слой атрибутов

Четвертый пункт - отдельный слой атрибутов поверх полей, который вешается
на поле в списке рядом с id и интерпретируется каждым адаптером по-своему.
Набор словарей расширяем - новый можно добавить, не трогая ядро; сегодня
реально работают `security` и `check`. Дальше всех продвинулся `security`,
и на нем хорошо видно, зачем вообще нужен отдельный слой атрибутов, а не
просто "сериализуй как есть".

Возьмем типичное событие авторизации - с PII и секретом внутри:

```cpp
struct AuthEvent {
    std::string user_email;
    std::string access_token;
    std::uint64_t duration_ns;

    CONTRACT(AuthEvent,
        (user_email, 1, contract::security::sensitive()),
        (access_token, 2,
            contract::security::secret(),
            contract::security::no_log(),
            contract::security::encrypt()),
        (duration_ns, 3)
    )
};
```

Один и тот же `AuthEvent`, без единого `if` в бизнес-коде, дальше по-разному
ведет себя в разных адаптерах:

- **JSON/console** - `sensitive()`/`secret()`/`no_log()` сами по себе ничего
  не прячут: это переключатель, который включает вызывающий код через
  `options` (по умолчанию `security_mode::ignore` - поле пишется как есть).
  В логи хотим `redact` для `user_email` (останется `"<redacted>"`, видно,
  что поле было, но не значение) и `omit` для `access_token` (пропадет из
  вывода целиком) - это выбор конкретного вызова, а не хардкод в структуре.
- **binary** - `encrypt()` работает иначе, безусловно: как только атрибут
  есть на поле, оно шифруется реальным ключом (`options.encrypt_key`) при
  каждой записи, без переключателя "включить/выключить".

На чистой сериализации так не вышло бы - там либо пишешь поле, либо нет,
для всего типа сразу. Здесь один `access_token` может быть зашифрован в
бинарном протоколе, спрятан в JSON-логах и виден только там, где явно
попросили видеть секреты - потому что политика живет на поле контракта, а
не размазана по коду каждого адаптера отдельно.

Так это выглядит не абстрактно, а в реальном структурированном логе
(упрощенный вариант `examples/logging.cpp`):

```cpp
struct SecretPayment {
    std::uint64_t order_id;
    std::string_view token;

    CONTRACT(SecretPayment,
        (order_id, 1),
        (token, 2, contract::security::secret()))
};

contract::logging::options opt{};
opt.json.secret = contract::adapters::json::security_mode::redact;
contract::logging::logger log{out, opt};

SecretPayment secret_payment{18, "tok_live_123"};
log.info("payment_sensitive", "Captured sensitive payment metadata",
    contract::logging::attribute("payment", secret_payment));
```

Фрагмент вывода (полную строку с `timestamp`/`context`/`severity` см. в
`examples/logging.cpp` - здесь для краткости оставлена только суть):

```json
{"name":"payment_sensitive","attributes":[{"name":"payment","value":{"order_id":18,"token":"<redacted>"}}]}
```

`token` вошел в структурированный лог как `"<redacted>"`, а не как реальное
значение - потому что так решил вызывающий код через `opt.json.secret`, а
не потому что кто-то в бизнес-логике не забыл его вручную вычистить.

Остальные словари - `unit`, `doc` - часть той же модели атрибутов, но
адаптеры пока не везде их учитывают - это следующий шаг, не готовая сегодня
фича.

## Не в ущерб скорости

Пятый и последний пункт - ничего из этого не должно создавать лишнюю
нагрузку на рантайм. Одна декларация вместо N×M - это, в первую очередь,
про удобство, но это не покупается ценой производительности:
protobuf-адаптер CONTRACT
сравнивали с настоящим libprotobuf на 14 сценариях - CONTRACT оказался
быстрее в 20 замерах из 28. Подробности, методология и два честных
исключения - в [отдельной статье про protobuf-адаптер](protobuf-adapter-performance.md).

## Чего CONTRACT не делает

Чтобы не создавать впечатления, что это решение "на все":

- не рантайм-рефлексия - обход полей раскрывается на этапе компиляции.
- не generic-сериализатор - формат и его правила целиком принадлежат
  адаптеру, ядро формат не выбирает и не диктует.
- не schema-first кодогенератор - нет отдельного файла схемы и шага
  генерации, схема - это сама C++-структура.
- не место для буферов, SQL или стороннего рантайм-кода - это
  ответственность конкретного адаптера, а не ядра.

## Пример: конфиг из YAML + дебаг-вывод

Напоследок - не абстрактный пример, а реальный код из репозитория
(`examples/yaml_file_read.cpp`): один и тот же контракт читает YAML-адаптер,
а печатает - debug-адаптер.

```cpp
struct PaymentConfig {
    std::string service;
    std::uint32_t port = 0;
    bool enabled = false;
    std::vector<std::string> tags;

    CONTRACT(PaymentConfig,
        (service, 1),
        (port, 2),
        (enabled, 3),
        (tags, 4))
};

contract::adapters::yaml::reader<contract::io::file_buffer_input> in(
    contract::io::file_buffer_input{"payment_config.yaml"});

PaymentConfig config{};
in >> config;

contract::cout.debug() << config;
```

При таком `payment_config.yaml`:

```yaml
service: payment
port: 8080
enabled: true
tags:
  - api
  - payments
  - production
```

вывод - реальный, снят с собранного бинарника, не придуманный:

```text
PaymentConfig:
  service: "payment"  # #1 std::string
  port: 8080          # #2 u32
  enabled: true       # #3 bool
  tags:               # #4 std::vector<std::string>, size=3
    - "api"           # [0]
    - "payments"      # [1]
    - "production"    # [2]
```

Тот же `PaymentConfig`, тот же контракт - id и тип каждого поля попадают в
вывод сами, без единой строчки кода, написанной специально под форматирование.

## А как же C++26 reflection?

Естественное возражение на этом месте: скоро в C++26 появится static
reflection - зачем тогда вообще макрос?

C++26 reflection умеет перечислять члены структуры без макроса -
теоретически это могло бы упростить перечисление физических членов, которое
сегодня руками пишет `CONTRACT(...)`. Но не факт, что это упростит описание
контракта в целом: id и атрибуты все равно нужно куда-то прикрепить, а
рефлексия сама по себе для этого ничего не дает - она отражает то, что уже
есть в структуре, а не декларирует, каким полю положено оставаться
(стабильный id из раздела про эволюцию схемы) или как с ним должен
обращаться внешний код (`security::secret()`, `schema::reserved_id()`).
Плюс `PROPERTY` - поле, за которым вообще нет физического члена, -
рефлексии там структурно нечего отражать, это объявленное поведение, а не
данность структуры. Не исключено, что итоговая запись с рефлексией вышла бы
не короче сегодняшнего макроса - просто по другой причине. Reflect-cpp уже
сегодня показывает предел рефлексионного подхода - у него нет ни
стабильного id, ни attribute-слоя, ни вычисляемых полей.

Второе, тоже ожидаемое возражение: макросы - это плохая практика, зачем
вообще так писать в 2026. Отчасти справедливо - текстовая подстановка без
области видимости, нечитаемые ошибки компиляции при неправильном использовании, это
реальная цена, а не выдумка противников. Но `CONTRACT(...)` - не макрос,
который прячет логику или control flow; он генерирует декларативные
дескрипторы полей, тем же путем, что `Q_OBJECT` в Qt, `TEST(...)` в gtest
или `BOOST_DESCRIBE_STRUCT` в Boost.Describe - устоявшийся паттерн именно
для этой задачи, а не изобретение CONTRACT. И пока static reflection не
стала мейнстримом (а даже когда станет - см. выше, id и атрибуты она все
равно не даст), это самый практичный инструмент, чтобы объявить метаданные
поля один раз, в самом C++-типе.

## Итог

Отправная точка была простой: взять удобство "одна декларация - много
форматов" из reflect-cpp и идею protobuf про стабильный id поля, который не
меняется при эволюции схемы, и соединить их в одном месте. По пути
выяснилось, что этого мало - контракту нашлась вторая работа: политика,
которую разные адаптеры трактуют по-своему. Так родились те пять пунктов из
начала статьи - и под каждым лежит рабочий код, а не одна формулировка.

Смысл контракта простой: объявил схему один раз рядом с типом - и дальше
делаешь с данными что хочешь: пишешь в бинарный протокол или protobuf,
читаешь конфиг из YAML, выводишь в дебаг или структурированный лог - без
вороха сервисных функций под каждую задачу и без ручного per-format
мэппинга полей. И это не в ущерб скорости - адаптеры работают быстро.

Собственно то, ради чего все затевалось: один контракт, любой формат, ноль
мэппинга.

Код - [github.com/antako76/Contract](https://github.com/antako76/Contract).
